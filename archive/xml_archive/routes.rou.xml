<routes>
    <!-- Define a car type -->
    <vType id="car" accel="2.6" decel="4.5" length="5" maxSpeed="13.9"/>

    <!-- Define a route (uses your road) -->
    <route id="route0" edges="E6"/>

    <!-- Add cars -->
    <vehicle id="car0" type="car" route="route0" depart="0"/>
    <vehicle id="car1" type="car" route="route0" depart="5"/>
</routes>
